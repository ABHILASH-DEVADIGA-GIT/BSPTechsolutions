// vite.config.ts
// ---------------------------------------------------------------
// Kept ONLY for IDE tooling (VS Code Vite extension, Vitest, etc.)
// Vinxi / TanStack Start uses app.config.ts as its authoritative
// config. Adding tanstackStart() or tanstackRouter() here caused
// the "no vinxi app config found" error — do not add them back.
// ---------------------------------------------------------------
import { defineConfig } from "vite";

export default defineConfig({});
